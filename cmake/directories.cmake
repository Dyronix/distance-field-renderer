# -----------------
# Directories       
# ----------------- 
SET (SRC_DIR source)
SET (EXTERNALS_DIR extern)
SET (MODULES_DIR modules)
SET (BIN_DIR bin)

SET (OUTPUT_BINDIR "${PROJECT_BINARY_DIR}/${BIN_DIR}")